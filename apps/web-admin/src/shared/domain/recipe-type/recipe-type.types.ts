export type RecipeType = {
  id: string;
  code: string;
  name: {
    en: string;
    ua: string;
  };
};

export type RecipeCuisine = {
  id: string;
  code: string;
  name: {
    en: string;
    ua: string;
  };
};

export type RecipeDietaryTag = {
  id: string;
  code: string;
  name: {
    en: string;
    ua: string;
  };
};
